name = "decarsauto"
compatibility_date = "2024-01-01"

# Cloudflare Pages 配置
pages_build_output_dir = "."

[site]
bucket = "."

# 自定义域名配置
# 在 Cloudflare Dashboard 中添加: decarsauto.com 和 www.decarsauto.com

# 环境变量（如果需要）
[vars]
ENVIRONMENT = "production"
DOMAIN = "decarsauto.com"
